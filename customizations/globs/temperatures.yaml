---
sensor.*_temperature:
  templates:
    icon_color: >
      if (state == 'unavailable') return 'brown';
      if (state == 'unknown') return 'brown';
      var val = Number(value);
      if (val < 7) return 'blue';
      if (val < 15) return 'cyan';
      if (val < 23) return 'green';
      if (val < 27) return 'yellow';
      return 'red';
    hide_attributes:
      - icon_color
      - templates
