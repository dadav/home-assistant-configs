---
# The Input Text helper integration allows you to define a text
# that can be controlled via the user interface and can be
# used within conditions of automation.
#
# In the UI these can be found as "Helpers". The input date time would be known
# as the Text helper.
#
# https://www.home-assistant.io/integrations/input_text/
#
input_text: !include_dir_named ../entities/input_text
